services:
  mongo:
    image: mongo
    restart: always
    env_file:
      - .env
    ports:
      - 27017:27017

  mongo-express:
    image: mongo-express
    restart: always
    ports:
      - 8081:8081
    depends_on:
      - mongo
    env_file:
      - .env